Selenium-
=========
mkdir ~/Group Creation
# Creates a directory for your project called "Group Creation" in your user directory

cd ~/Group Creation
# Changes the current working directory to your newly created directory

git init
# Sets up the necessary Git files
# Initialized empty Git repository in /Users/you/Group Creation/.git/

touch README
# Creates a file called "README" in your Group Creation directory
